enum TimerMode {
    // 正计时
    positive,
    // 倒计时
    reverse,
    // 日期模式
    nomal,
}
enum TimerStatusMode {
    // 开始
    start,
    // 停止
    stop,
    // 超时
    timeout,
}

export { TimerMode, TimerStatusMode };
